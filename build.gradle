allprojects {
    repositories {
        mavenCentral()
        maven { url "https://dl.bintray.com/reactivex/RxJava/" }
    }
}

tasks.named('wrapper') {
    distributionType = Wrapper.DistributionType.ALL
    gradleVersion = "7.0"
}